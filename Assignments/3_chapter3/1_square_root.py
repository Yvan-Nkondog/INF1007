# Write a program that calculate the square root

import math

def compute_square_root(n : float) -> float:
    return math.sqrt(n)

if __name__ == "__main__":
    print(compute_square_root(25))
    print(compute_square_root(10.5))